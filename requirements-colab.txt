# Colab/Kaggle install — DO NOT pin torch/torchvision here.
# The host image ships a CUDA-matched torch; reinstalling it breaks torchvision.
# Only the fine-tune + serve libs, resolved against the host torch.
# NOTE: the version caps below pull torch down to 2.5.1, which mismatches
# Colab's prebuilt torchvision 0.26 (built for torch 2.11) -> the notebook
# uninstalls torchvision after this install (text-only, not needed).
transformers>=4.44,<4.50
peft>=0.12,<0.15
bitsandbytes>=0.43,<0.45
trl>=0.9,<0.13
datasets>=2.20,<3.3
accelerate>=0.33,<1.3
wandb>=0.17
anthropic>=0.39
scikit-learn>=1.4
