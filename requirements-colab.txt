# Colab/Kaggle install — DO NOT pin torch/torchvision here.
# The host image ships a CUDA-matched torch; reinstalling it breaks torchvision.
# Only the fine-tune + serve libs, resolved against the host torch.
#
# NO UPPER CAPS: upper bounds pulled torch down to 2.5.1 and pinned an old
# bitsandbytes that imports the removed `triton.ops` and ships no cuda128
# binary -> crashes on Colab's current image (torch 2.11 / CUDA 12.8 / new
# triton). Letting pip take the latest keeps the host torch and a bnb that
# matches the CUDA toolkit. bitsandbytes floor raised past the triton.ops break.
transformers>=4.44
peft>=0.12
bitsandbytes>=0.45
trl>=0.9
datasets>=2.20
accelerate>=0.33
wandb>=0.17
anthropic>=0.39
scikit-learn>=1.4
